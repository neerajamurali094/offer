import com.diviso.graeshoppe.offer.model.OrderModel;

rule "Is valid Offer"
	when
     	order:OrderModel(getOrderTotal() > 99)
 	then
       order.setOrderDiscountPercentage(40L);
      System.out.println("Apply 20% discount for order over $99:");
      System.out.println(order);
 end